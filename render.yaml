services:
  - type: web
    name: credito-bancolombia
    runtime: static
    buildCommand: echo "static site"
    staticPublishPath: .
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
